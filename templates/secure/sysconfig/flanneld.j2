# {{ ansible_managed }}
# Flanneld configuration options

# etcd url location.  Point this to the server where etcd runs
FLANNEL_ETCD="https://{{ ansible_hostname }}.{{ ansible_domain }}:{{ etcd_port }}"

# etcd config key.  This is the configuration key that flannel queries
# For address range assignment
FLANNEL_ETCD_KEY="{{ etcd_key }}"

#secure etcd settings
FLANNELD_ETCD_CAFILE={{ k8s_apiserver_cert_path }}/{{ k8s_apiserver_ca_cert_filename }}
FLANNELD_ETCD_CERTFILE={{ k8s_apiserver_cert_path }}/{{ k8s_apiserver_server_cert_filename }}
FLANNELD_ETCD_KEYFILE={{ k8s_apiserver_cert_path }}/{{ k8s_apiserver_server_key_filename }}

# Any additional options that you want to pass
#FLANNEL_OPTIONS=""

